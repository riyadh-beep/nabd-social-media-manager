YªçŠx-®éÜj×¢ëiºÚ+Š§j[h‘éÜ¢éíë^¢Ö¥¢ëiºÙbë5import assert from 'node:assert/strict';
import { createServer } from 'node:http';
import { once } from 'node:events';
import test from 'node:test';
iç^-¢G§²ÚîÆ­yÓeAllConnections();
    await Promise.all([new Promise(resolve => relay.close(resolve)), new Promise(resolve => upstream.close(resolve))]);
  }
});
