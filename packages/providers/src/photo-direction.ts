YªçŠx-®éÜj×¢ëiºÚ+Š§j[h‘éÜ¢éí÷“¢Ö¥¢ëiºÙbë5import { z } from "zod";
import type { AppEnvironment } from "../../config/src/env.js";

export type PhotoCandidate = { id: string; knowledgeId: strinó­¢G§²ÚîÆ­yÒ("Unknown photo");
    return result;
  } catch { throw new Error("AI could not validate the product photo analysis. No drafts were saved."); }
}
