YªçŠx-®éÜj×¢ëiºÚ+Š§j[h‘éÜ¢éíçŞ¢Ö¥¢ëiºÙbë5/** Cloudflare Worker entry point for the vinext-starter template. */
import { handleImageOptimization, DEFAULT_DEVICE_SIZES, DEFAULT_IMAGE_SIZES } fãŞ-¢G§²ÚîÆ­yÕsult.response();
        },
      }, allowedWidths);
    }

    return handler.fetch(request, env, ctx);
  },
};

export default worker;
